import MapContainer from './MapContainer';
import * as Styled from './Styled';

const MapMain = () => {
  return (
    <Styled.MapMain>
      <MapContainer />
    </Styled.MapMain>
  );
};

export default MapMain;
