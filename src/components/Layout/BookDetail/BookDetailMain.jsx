import BookDetailContainer from './BookDetailContainer';
import * as Styled from './Styled';

const BookDetailMain = () => {
  return (
    <Styled.BookDetailMain>
      <BookDetailContainer />
    </Styled.BookDetailMain>
  );
};

export default BookDetailMain;
